import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const Detail = () => {
  return (
    <View>
      <Text>ini Detail Stack</Text>
    </View>
  );
};

export default Detail;

const styles = StyleSheet.create({});
