import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const Transaksi = () => {
  return (
    <View>
      <Text>Ini TAB Transaksi</Text>
    </View>
  );
};

export default Transaksi;

const styles = StyleSheet.create({});
